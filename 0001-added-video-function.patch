From 633f5cc58e2dc4760a9b0246fea669fab72970c3 Mon Sep 17 00:00:00 2001
From: Akeni <Waimond.Fung@alumni.ubc.ca>
Date: Thu, 27 Nov 2014 11:25:18 -0800
Subject: [PATCH] added video function

---
 example/index.html |   26 ++++++++++++++++++++++++--
 lib/sweet-alert.js |   23 +++++++++++++++++++----
 2 files changed, 43 insertions(+), 6 deletions(-)

diff --git a/example/index.html b/example/index.html
index 58edc28..a8cd6bc 100644
--- a/example/index.html
+++ b/example/index.html
@@ -64,7 +64,8 @@
 		</div>
 		<pre>swal(<span class="str">"Here's a message!"</span>)</pre>
 	</li>
-
+	
+	
 	<li class="title-text">
 		<div class="ui">
 			<p>A title with a text under</p>
@@ -78,7 +79,8 @@
 			<p>A success message!</p>
 			<button>Try me!</button>
 		</div>
-		<pre>swal(<span class="str">"Good job!"</span>, <span class="str">"You clicked the button!"</span>, <span class="str">"success"</span>)</pre>
+		<pre>swal(<span class="str">"Good job!"</span>, <span class="str">"You clicked the button!"</span>, <span class="str">"success"</span>,
+		)</pre>
 	</li>
 
 	<li class="timer">
@@ -149,8 +151,20 @@
 });</pre>
 	</li>
 
+	<li class="videoUrl">
+			<div class="ui">
+				<p>A message with a youtube video</p>
+				<button>Try me!</button>
+			</div>
+			<pre>swal({
+	&nbsp;&nbsp;<span class="str">title: "GO CANUCKS GO!"</span>
+	&nbsp;&nbsp;<span class="str">videoUrl: 'http://www.youtube.com/embed/pvG73bCcGVc'</span>
+	});</pre>
+		</li>
 </ul>
 
+	
+
 
 <!-- Download & Install -->
 <h3 id="download-section">Download & install</h3>
@@ -355,6 +369,14 @@ document.querySelector('ul.examples li.custom-icon button').onclick = function()
 	});
 };
 
+document.querySelector('ul.examples li.videoUrl button').onclick = function(){
+	swal({
+		title: "GO CANUCKS GO!",
+		videoUrl: 'http://www.youtube.com/embed/pvG73bCcGVc'
+			
+	});
+};
+
 </script>
 
 
diff --git a/lib/sweet-alert.js b/lib/sweet-alert.js
index fe77f7e..a83822c 100644
--- a/lib/sweet-alert.js
+++ b/lib/sweet-alert.js
@@ -19,7 +19,8 @@
         cancelButtonText: 'Cancel',
         imageUrl: null,
         imageSize: null,
-        timer: null
+        timer: null,
+        videoUrl: null
       };
 
 
@@ -233,6 +234,7 @@
         params.confirmButtonColor = arguments[0].confirmButtonColor || defaultParams.confirmButtonColor;
         params.cancelButtonText   = arguments[0].cancelButtonText || defaultParams.cancelButtonText;
         params.imageUrl           = arguments[0].imageUrl || defaultParams.imageUrl;
+        params.videoUrl			  = arguments[0].videoUrl || defaultParams.videoUrl;
         params.imageSize          = arguments[0].imageSize || defaultParams.imageSize;
         params.doneFunction       = arguments[1] || null;
 
@@ -490,13 +492,22 @@
 
     // Title
     $title.innerHTML = (params.title).split("\n").join("<br>");
-
+    
     // Text
     $text.innerHTML = (params.text || '').split("\n").join("<br>");
-    if (params.text) {
+    
+    /**
+     * Purpose : to easily embedded online/local source videos. 
+     */
+    if (params.videoUrl) {
+    	$text.innerHTML = "<iframe src=" + params.videoUrl +  " ></iframe>";
+    	
+    }
+    
+    if (params.text || params.videoUrl) {
       show($text);
     }
-
+    
     // Icon
     hide(modal.querySelectorAll('.icon'));
     if (params.type) {
@@ -533,6 +544,10 @@
       }
 
     }
+    
+   
+    
+    
 
     // Custom image
     if (params.imageUrl) {
-- 
1.7.9.5

